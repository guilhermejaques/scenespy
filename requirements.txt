# Dependencies for running every Scenespy mode from source or building a full
# distribution.
-r requirements-base.txt
-r requirements-ai.txt

# External executables required at runtime:
# - ffmpeg
# - ffprobe
#
# CUDA face detection builds:
# Use scripts/install_build_dependencies.py --torch-mode auto|cpu|cuda before
# packaging so the release contains the intended PyTorch wheel.
