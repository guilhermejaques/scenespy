# AI dependencies for running Detect faces from source.
#
# In packaged releases, install_runtime.py installs torch/torchvision from the
# official PyTorch CPU or CUDA index and installs the remaining packages from
# its pinned package list.
torch==2.5.1
torchvision==0.20.1
ultralytics==8.4.9
mediapipe==0.10.9
